        // Rebind and reuse cached generic-instance Il2CppClass objects whose type
        // definition belongs to the given image after a hot-update reload. AOT
        // generic instances are left untouched. This fixes stale lazy-built caches
        // (interfaceOffsets, vtable, implementedInterfaces, methods, fields, etc.)
        // while preserving Il2CppClass* identity.
        static void ResetGenericInstanceCaches(const Il2CppImage* image, bool resolveRelations = true);